## Output Format for Executable Code

**CRITICAL: This request expects executable Emacs Lisp code as output.**

Rules:
1. Return ONLY valid Emacs Lisp code that can be executed directly
2. Do NOT include explanations, comments, or markdown formatting
3. Do NOT wrap code in ```lisp blocks
4. Ensure all parentheses are balanced
5. Use only functions and variables available in current Emacs session
6. Code should be safe to execute and not cause system damage
7. The output will be evaluated using `eval-buffer`, so syntax must be perfect

Example valid outputs:
- `(message "Hello World")`
- `(setq my-var 42)`
- `(defun my-function () (interactive) (message "Done"))`

Invalid outputs:
- Any text explanation
- Markdown code blocks
- Malformed Lisp expressions
